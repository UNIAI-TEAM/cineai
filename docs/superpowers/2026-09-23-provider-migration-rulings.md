# Bỏ TokenFree — quyết định (ruling) trong lúc thực thi Plan A/B/C

Ghi lại mọi quyết định controller đưa ra thay người dùng khi chạy subagent-driven-development (trích nguyên văn từ ledger). Mỗi dòng: quyết định — lý do — cái giá nếu sai.

## 2026-09-22-provider-migration-a-backend-core

- | T5 ↔ T7 (presets) | T7 imports `VOLC_TTS_STATIC_MODELS`, `BYTEPLUS_TTS_URL` from volc_tts_adapter; T5 interface lists BYTEPLUS_TTS_URL only | **Ruling:** T5 must export `VOLC_TTS_STATIC_MODELS` (list of 3 dicts used by list_models) — carried into T5 dispatch — cost if wrong: T7 import error, trivial fix |
- | T6 ↔ T7 (schemas_routing) | T6 deletes LogicalModel/DefaultModels; model_settings.py + model_routing_config.py still import them until T7 → app import breaks between T6 and T7 | **Ruling:** T6 keeps legacy classes/functions (LogicalModelBinding, LogicalModel, DefaultModels, default_models_to_dict/from_dict, and model_routing_config sync helpers) in place, only ADDS new types; deletion moves to T7 together with the model_settings rewrite. Spec 5.3 unchanged in outcome — cost if wrong: dead code lingers one task |
- | Global "file ≤ 500 lines" ↔ T8 media_gateway (moves ~700 lines) | STANDARDS says 尽量 | **Ruling:** media_gateway may exceed 500 (target ≤ 750); record as deferred minor for a later split — cost if wrong: readability only |
- Task 1: Ruling: chat_storyboard delegate `*args` vs keyword-only target (plan-mandated snippet) — make kepu_text.chat_storyboard keep the original 7 positional-or-keyword params, `mock` keyword-only; add smoke test with positional call — cost if wrong: only positional callers break (none today)
- Task 1: Ruling: commit trailer says "Claude Sonnet 5" (subagent auto-trailer) — amend unpushed commit to the required Fable 5.1 trailer; all later implementer dispatches must state the exact trailer and forbid harness auto-trailer drift — cost if wrong: attribution only
- Task 4: Ruling: put "sora" into the shared classifier `infer_model_capability` video keywords (model_routing_config.py) and drop the adapter-local skip — fixes catalog/bindings validation too — cost if wrong: a text model named "*sora*" would be misclassified (none known)
- Task 6: Ruling: fix both — parse treats non-dict slots/overrides as empty; validate reports "Slot '<key>' không hợp lệ" instead of KeyError — cost if wrong: none (strictly more tolerant)
- Task 7: Ruling: logical_model_router.py becomes a thin compat shim over function_router in T7 (deleted in T10); tests test_logical_image_route.py + test_seedance_model_routing.py removed in T7 (they test deleted machinery); media_catalog gets a minimal adaptation in T7, full rewrite in T10 — cost if wrong: one task of dead shim code
- Task 7: Ruling: fix #1–#3 in fix round 1 (+ two cheap minors: duplicate provider id → ValueError; media_catalog default provider "" not "tokenfree")
- Task 7: Ruling: #4 parked — friendly aliases (seedream-4.5/seedance-2) are dropped by spec §5.1; users now pick real model ids; Task 10 must make resolve_seedream_model_endpoint/resolve_seedance_model_endpoint pass an ALLOWED requested id through unchanged and add a regression test for that — carried into T10 dispatch — cost if wrong: an old drama project storing "seedance-2" silently uses the slot default (acceptable)
- Task 8: Ruling: media_gateway.py 818 lines accepted (moved code); split (_write_mock_image → ark_mock.py, audio-fallback helper) recorded as deferred minor for final review — cost if wrong: readability only
- Task 8: Ruling (→ T10 obligation): call sites that pass a persisted user model id (kepu project.image_model/video_model, drama prepared.model_id) must degrade to `model=None` with a warning when `not is_model_allowed(function_id, id)` instead of failing with model.not_available; API/canvas requests where the user just picked the model keep the strict error — cost if wrong: old projects fail to regenerate until the user re-picks a model
- Task 8: Ruling: fix #1 (deterministic fallback via allowed_bindings()[0], resolved once per poll_task) and #3 (two tests) now, plus minors cost_fen-direct-call and redundant kepu_text import; #2 parked — plan-mandated in-memory map, T11 persists provider_channel_id and threads it into studio_tools/billing_util/drama jobs (already in plan; made explicit acceptance criterion) — cost if wrong: mis-routed polls only across process restarts until T11
- Task 9: Ruling (→ T10 obligation): TtsService.synthesize must resolve preset aliases (narrator_calm…) via volc_tts_adapter.SPEAKER_ALIASES before handing `voice` to adapters/edge-tts, since ark.py's inline voice_map disappears with the shim — cost if wrong: gender mis-inferred for alias voices
- Task 10: Ruling: fix Critical + #2 + #4 + minors 5/9/10/11 now; #3 → keep the sentence single-language Chinese ("上游模型服务"), Plan C localizes; add regression test walking _script_stage with stubbed chat_storyboard — cost if wrong: none
- Ruling: new commits from this session carry trailer 'Co-Authored-By: Claude Opus 5.5 (1M context)' instead of plan's Fable 5.1 — session model changed; attribution must be accurate — cost if wrong: attribution only
- Ruling: user (2026-09-23) asked to write + execute Plan B and Plan C with subagents without asking — Plan B plan written by opus subagent in parallel with Task 12 (plan file only, no commit); Plan C plan written after Plan B plan exists (consumes its admin API contract); work stays on feat/provider-migration, no push/merge to main without the user — cost if wrong: extra rework on the branch only
- Ruling: kepu e2e not reaching DONE accepted — failure is local ffmpeg build without libfreetype drawtext, all provider-layer stages (script/image/audio) passed — cost if wrong: a compose regression goes unnoticed until a machine with full ffmpeg runs it
- Ruling: Task 12 #2 (empty model catalog with zero providers) not fixed in Plan A — spec makes models admin-configured only; generation still works in mock with model=None (e2e proved). Carried into Plan C: frontend model pickers get an explicit empty state ('chưa cấu hình mô hình, liên hệ quản trị') and Plan C/B may add mock-mode catalog rows if cheap — cost if wrong: fresh no-key dev setups show empty dropdowns until Plan C lands
- Ruling: Plan B plan (docs/superpowers/plans/2026-09-23-provider-migration-b-billing.md, 8 tasks) accepted with writer's decisions: settlement from provider_rates only (no upstream quota); per_m_input_output 70/30 split when only total tokens; no invented seed-2-0 text rates (→ unpriced_models); flat per_image price (drops '2K' fallback); video token area by 16:9 tiers; TaskResult.model carries real model for pricing; finance keeps actual_cost_fen=cost_fen; admin validation 400 with Vietnamese detail (no new AppError); billing_kie_fen_per_credit kept unused until Plan C; Plan B Task 8 also fixes billing lines in CLAUDE.md/PROVIDERS.md (+ README.md:492 minor) — cost if wrong: pricing defaults need admin edits via model-rates endpoint
- Ruling: final fix wave = I-1..I-9 backend parts + M-1/M-7/M-8/M-9 (findings file final-review-findings.md); I-3/I-4 frontend parts, TaskRunBriefOut.provider_channel_id, dead flat fields, provider test copy, /tokenfree/quota caller, release note (M-6, M-10) → Plan C; M-2/M-3/M-4/M-5 stay deferred; LLM real-usage billing → Plan B pre-flight decision — cost if wrong: deferred minors surface as operator friction, none are data-loss or key-leak
- Ruling: accept fix-wave choices — providers_seeded set only after a seed that created ≥1 provider (keys added later still seed once); empty stored base_url + new URL w/o key → 400; /api/health models shape now {slot:{status,model}} (no consumer in frontend/admin/deploy); Volc preference keyed on requested voice via new is_volc_speaker heuristic — cost if wrong: an external health monitor parsing old flat labels breaks; exotic Volc voice ids may miss the Volc-first ordering
- Ruling: Plan C plan (docs/superpowers/plans/2026-09-23-provider-migration-c-admin-frontend.md, 12 tasks) accepted with writer's decisions: provider saves per-dialog, bindings save with page Lưu; UI blocks untick/delete of in-use models/providers; TS mirror of infer_model_capability; no new npm deps; new admin screens in Vietnamese, old panels stay Chinese except TokenFree strings; vi copy 'liên hệ với chúng tôi' per glossary; node --test for pure helpers (Node 25 TS strip); docs/releases/ is gitignored so release note stays local; real-key regression round left to the user — cost if wrong: UI-level rework in admin only
- Ruling: keep Plan A workspace (ledger) until Plan B and C finish so the full ruling list reaches the user in the final message; delete all three plan workspaces at the very end — cost if wrong: gitignored scratch lingers a few hours

## 2026-09-23-provider-migration-b-billing

- Ruling H-1 (→ T6): add real LLM usage: ContextVar in billing/context.py set by billing_scope; llm_client.chat_completions records data["usage"] + routed model (lazy import); record_line sums/prices per call; relabel-to-priciest stays only as fallback when no usage recorded; T8 doc updated — why: fixed 80k-token estimate per call misbills every text call — cost if wrong: ~60 extra lines, reversible
- Ruling M-2 (→ T4, test in T6): media_gateway remembers model at video create (same LRU as _task_channels); match_rate tries remembered model then echoed payload model; kepu passes channel_id for re-fetch; warn on settle fallback; +2 tests — why: ep-… ids / settings.model_video fallback misprice silently — cost if wrong: small extra state
- Ruling M-1 (T1): do NOT add seed-2-0-* default rate (no sourced price); it lands in unpriced_models and admin sets it via model-rates — cost if wrong: text on seed-2-0 billed by token fallback until admin prices it
- Ruling M-3 (T6 step 9): grep hit on billing_kie_fen_per_credit in config.py / schemas_settings.py is expected; do not remove the field
- Ruling M-3b (T1/T5): keep volc_tts bindings visible in unpriced_models (honest signal to admin) — preflight suggested hiding; rejected because hiding an unpriced model hides a billing gap — cost if wrong: one noisy row in the admin list
- Ruling M-4 (T1/T3/T6): share one helper each for usage-block unwrap, token price (video_fen reuses _token_price), image fallback (freeze+settle) instead of duplicating
- Ruling L (all): fix test-count/test-name/line-count cosmetics as encountered; T2 appends to fix-wave docstring (not replace); T4 step 10(b) insert after docstring, match indentation; T5 schema max_length align to validator 128; strengthen fragment-duration test to fen == 1958; hoist routing-isolation fixture into conftest; add provider_rates: [] to fix-wave scrub test input
- Ruling L-12 (T6/T7): delete tests/test_tokenfree_usage.py in T7 together with the module (it holds the I-9 regression); T7 goal worded as cleanup (leak already closed by Plan A fix wave)
- Ruling L-6/13/14 (T8): docs cite source for 6240 tokens and Seedance video-token formula, document usd=0 → token fallback, note TTS label gap as known limitation; also fix docs/PROVIDERS.md ~218 glued paragraph (Plan A deferred minor) and README.md:492 billing sentence
- Ruling (→ T6 obligation): (1) a 0-fen upstream image cost (generated_images:0) must settle at 0, not fall back to the estimate — record_seedream_image_usage / charge_fen_for_usage must distinguish 'cost 0' from 'no cost'; (2) charge_fen_for_usage applies the two-candidate model order (create-time model then echoed) via provider_rates.first_priced_model; (3) record_seedance_video_usage usage unwrap moves to usage_block when T6 rewrites it — cost if wrong: failed images billed at estimate, video mispriced on ep- ids
- Ruling L-9 (T5, supersedes 'align to 128' in pre-flight line): drop schema max_length on ProviderRateRow.pattern so over-long patterns get the validator's Vietnamese 400 (body bounded by MAX_RATE_ROWS) — why: contract says 422 only for JSON type errors — cost if wrong: very large pattern strings reach the validator
- Ruling (→ T7 add-on): (a) captured LLM lines labelled basis 'upstream_usage' not 'upstream_cost' (usage.py:36 writes cost_fen → mislabel 实测(费用)); (b) llm_client records calls without usage too (usage=None) and _captured_llm_usage prices those at the per-call estimate so a mixed scope doesn't drop them; + tests for mixed scope, gather child writing parent list, closed scope not leaking — cost if wrong: small under-charge on mixed scopes / misleading admin label
- Ruling: docs/BILLING.md stays Chinese (existing file language, brief text is Chinese); controller dispatch saying 'Vietnamese' was wrong — cost if wrong: none
- Ruling: Task 8 Important + 2 minors folded into the Plan B final-review fix wave (one fix dispatch) instead of a separate fix round, since the final review runs concurrently and a separate docs commit would race it — cost if wrong: none (docs only)
- Ruling: fix wave = I-1 + M-1..M-6 + T8 Important/minors + concurrency test (final-review-findings.md); Minor 7 (gpt-image input tokens, multi-image overage) recorded for pricing review; test gap 8a deferred; Plan C gaps → carry file — cost if wrong: minor pricing gaps remain documented
- Ruling: accept I-1 dedup design — with provider_task_id dedup per user across TaskRuns (a re-run drama handler that submits a NEW upstream task is billed again: real upstream cost, may hit uncapped overage); without provider id dedup key adds shot_id so mock/no-id kepu shots each bill — cost if wrong: a retried drama fragment bills twice where users expect one charge per fragment

## 2026-09-23-provider-migration-c-admin-frontend

- Ruling H1 (T12): vite configs read optional VITE_CACHE_DIR (both apps); T12 points it to the scratchpad so worktree Vite never rebuilds the shared cache under the user's running 5173/5174; npm install/ci forbidden in the worktree — cost if wrong: user's dev servers re-optimize deps
- Ruling M1 (T5 step 9): grep 'tokenfree/quota' not 'quota' (live quota_* fields exist)
- Ruling M2 (T6 step 2): reword AdminModelSettings comment so it doesn't contain ark_api_key/ark_base_url (task's own grep)
- Ruling M3 (T7): add provider_channel_id also to AdminTaskBriefOut (projects.py:154, drama_projects.py:247) + test — admin recent_tasks uses it — cost if wrong: small backend schema addition
- Ruling M4 (T12): build printfilm_plan_c URLs inline per Bash call; verify current_database()
- Ruling M5 (T12): vite --host 127.0.0.1 for both apps
- Ruling M6/M7 (T12): smoke backend with REDIS_URL=redis://127.0.0.1:1/0 and PUBLIC_BASE_URL=http://127.0.0.1:8765
- Ruling Low: T1 has 23 tests; skip T10 README §6.5 edit (Plan B rewrote it); reconcileCatalogModel compares case/whitespace-insensitively like backend; T12 health wait checks "ok"; T11 final typecheck uses tsc -b --force; T10 release note includes Plan B pricing caveats (gpt-image input tokens unbilled, multi-image Seedream overage, settle may exceed freeze, TTS label gap)
- Ruling rubric: use CAPABILITIES.length not /4; constants for weight bounds 1..100; modelProviderLabel in MediaModelGrid; if cheap build unit suffixes from API units and extract the triplicated "Tự động" option; accept key={i} rows, exact-match pendingUnpriced, admin copies of backend logic (with comments)
- Ruling (→ T3/T4): user-visible Vietnamese admin copy says 'nhà cung cấp' (glossary: upstream provider → nhà cung cấp mô hình), not the loanword 'provider'; T4 sweeps Task 1–4 strings (providerRouting.ts labels, search placeholder) — code identifiers unchanged — cost if wrong: copy churn only
- Task 4: parked — Important (inherited API contract): full-list provider PATCH from a stale snapshot can delete a provider added in another tab/session (no updated_at check) — Ruling: fix in Plan C final fix wave with a cheap mitigation (re-GET routing right before saveProviders and merge the edit into the fresh list; optionally compare updated_at and warn) — why: narrow concurrent-admin window, not introduced by T4 — cost if wrong: a concurrently added provider is silently deleted
- Ruling: 6fd5393 carries 'Co-Authored-By: Claude Sonnet 5' (subagent's own model) instead of Opus 5.5 — accepted, not amended (accurate attribution, unpushed) — cost if wrong: attribution only
- Task 6: Important (plan-mandated): RuntimeSettingsPanel status-bar copy 'thêm provider' contradicts nhà cung cấp ruling — Ruling: one-line copy fix carried into T7 dispatch — cost if wrong: none
- Ruling (recorded retroactively, was in T8 dispatch): vi empty-state copy 'Chưa có mô hình nào được cấu hình — hãy liên hệ với chúng tôi' for both image/video (glossary 管理员→chúng tôi)
- Ruling (→ final wave): empty-state copy must NOT claim 'vẫn tạo được / you can still generate' — when catalog is empty allowed_bindings is empty and generation fails (outside mock); reword in 3 locales to just say not configured + contact — cost if wrong: misleading reassurance
- Ruling: legalContent processors section stays generic ('nhà cung cấp mô hình'), not naming OpenAI/BytePlus — legal/product decision left to the user, surfaced in final summary — cost if wrong: privacy text less specific than a lawyer may want
- Ruling: Plan C fix wave = I1, I2, I3, M1, M2, M6, M7, B2a (sanitize user-visible drama job errors), B1 if cheap, delete stray mock mp3 (final-review-findings.md); deferred: M3, M4, M5, stale bindings save race, LLM mock branch, billing-history label i18n, legal processor naming — cost if wrong: deferred items are UX polish / mock-only
- Ruling: follow-ups not fixed in this branch — (a) seed-assets job still stores raw exception text (jobs.py:1894 / set_seed_error); (b) new drama user_job_error short messages are fixed Vietnamese strings, zh/en UIs show Vietnamese (should become translated AppError codes) — why: outside finding list, i18n wave — cost if wrong: raw upstream text still visible on seed failures; zh/en users see Vietnamese error text

## Việc còn lại (deferred / parked)

- Task 1: minor (deferred): kepu_text.py 596 lines (>500 target); no unit test for chat_storyboard/parse_storyboard prompt path (pre-existing gap)
- Task 2: minor (deferred): 3 docstrings in providers/base.py translated to English instead of verbatim Chinese; registry.url_needs_auth non-functional until adapters land (T3–T5)
- Task 3: minor (deferred): ark_adapter.py 560 lines (>500); create_video tiers assume content[0]/[1] exist (inherited from ark.py)
- Task 4: minor (deferred): image-size clamp can overshoot 3:1 by <1% on absurd WxH; Chinese vs English error-string style differs between adapters
- Task 5: minor (deferred): parse_openspeech_ndjson dropped a log-only warning branch; loose `Any` type hints on 3 unsupported stubs
- Task 7: Ruling: #4 parked — friendly aliases (seedream-4.5/seedance-2) are dropped by spec §5.1; users now pick real model ids; Task 10 must make resolve_seedream_model_endpoint/resolve_seedance_model_endpoint pass an ALLOWED requested id through unchanged and add a regression test for that — carried into T10 dispatch — cost if wrong: an old drama project storing "seedance-2" silently uses the slot default (acceptable)
- Task 7: minor (deferred): deleting all providers re-seeds from env (document in PROVIDERS.md); stale Chinese comments in api/admin/settings.py:46/56; channel_supports_model unused (delete in T10); long lines/semicolons in model_settings.py; test_model_settings_bootstrap not isolating .env (pass _env_file=None); .env.example has two `replace-me` keys; _get_or_create_app_row writes flat unencrypted on first creation (pre-existing); admin UI dead until Plan C
- Task 7: complete (commits 654391b..38d2cfb, review clean; 1 parked ruling → T10 obligation)
- Task 8: Ruling: fix #1 (deterministic fallback via allowed_bindings()[0], resolved once per poll_task) and #3 (two tests) now, plus minors cost_fen-direct-call and redundant kepu_text import; #2 parked — plan-mandated in-memory map, T11 persists provider_channel_id and threads it into studio_tools/billing_util/drama jobs (already in plan; made explicit acceptance criterion) — cost if wrong: mis-routed polls only across process restarts until T11
- Task 8: minor (deferred): Retry-After backoff lost in poll loop; ProviderNotSupported aborts failover chain; _task_channels never evicts; legacy ids → T10 ruling
- Task 8: minor (deferred): gating test uses single-binding fixture (weak pass-through assertion); poll fallback slot hardcoded kepu.video; media_gateway.py 823 lines
- Task 8: complete (commits 38d2cfb..f18f9ed, review clean; 1 parked → T11)
- Task 9: minor (deferred): lazy TtsService import in media_gateway.tts; no delegation test for MediaGateway.tts; _persist_mp3 ffmpeg branch untested (pre-existing)
- Task 10: minor (deferred): is_valid_project_media_model strict vs pipeline degradation (PATCH project with legacy id → 400); tokenfree_pricing fallback hardcodes size "2K"; media_catalog union fetches snapshot 6×
- Task 11: minor (deferred): TaskRunBriefOut lacks provider_channel_id; ownership lookups by provider_task_id use .limit(1) without order_by (pre-existing)
- Task 12: minor (deferred): README.md:492 '按 TokenFree 官方成本' billing sentence confusing next to new wording (Plan B owns billing docs)
- Task 12: minor (deferred → Plan C): admin RoutingSettingsPanel.tsx TOKENFREE_BASE_URL/CONSOLE_URL + lib/tokenfreeRecommendedModels.ts (sent to Plan C writer)
- Final review: minor (deferred → Plan B Task 8): docs/PROVIDERS.md ~218 old 'AdminRoutingSettingsSaveOut.applied' paragraph glued onto the Volc TTS bullet
- Final review: minor (deferred): drama/generation.py:1297 Seedream size computed for bindings[0] while weighted pick may choose another model (ark_adapter re-clamps); same_host('x','') True for empty stored URL; legacy detection case-sensitive 'tokenfree.com'; SystemModelChannelIn.id overlong → 422 not 400
- Task 1: minor (deferred): usage_block/token_price_usd lack direct unit tests; rate_cost_usd treats explicit generated_images:0 as 1 (brief-mandated) → carry to T6 (settle must not charge an image for a failed 0-image result)
- Task 2: minor (deferred): parse_provider_rates drops corrupted stored rows silently (no logger.warning) — consider warning in T5/T6 if touched
- Task 3: minor (deferred): rate_quotes._max_fen evaluates price('') eagerly as max() default (wasted work, latent fragility); priced_routing fixture a second isolation pattern w/o get_settings.cache_clear
- Task 6: minor (deferred → T8 docs): H-1 can make settle exceed freeze (retries/seed calls swept into next line, uncapped overage path settlement.py:297 — existing); zero-image on unpriced model = 36 (unreachable via gateway); priciest_model duplicates _max_fen logic (brief-mandated)
- Final review (Plan B): minor (deferred): dedup query ORDER BY id + non-sargable LIKE may seq-scan on miss (drop ORDER BY or bound by created_at); dedup keyed also on billing_key (video0/video1 same provider task double-record, pre-existing); check-then-insert race w/o unique constraint (pre-existing)
- Task 2: minor (deferred): ChevronRight in overrides toggle lacks aria-hidden; 900px settings-providers-layout rule belongs to T4 (check there)
- Task 3: minor (deferred): enabled-model count not deduped; stale preset_id after protocol change on unsaved draft (graceful)
- Task 4: parked — Important (inherited API contract): full-list provider PATCH from a stale snapshot can delete a provider added in another tab/session (no updated_at check) — Ruling: fix in Plan C final fix wave with a cheap mitigation (re-GET routing right before saveProviders and merge the edit into the fresh list; optionally compare updated_at and warn) — why: narrow concurrent-admin window, not introduced by T4 — cost if wrong: a concurrently added provider is silently deleted
- Task 4: minor (deferred): failed initial load leaves Mô hình tab on a spinner (same pattern as Runtime/Oss panels) — add error+retry state in final wave if cheap
- Task 4: complete (commits 437f3e9..c590668, review clean, 1 parked)
- Task 5: minor (deferred): PaymentSettingsPanel 508 lines; key={i} on reorderable rows (focus jump); pendingUnpriced exact-match (UI hint added)
- Task 7: minor (deferred): AdminTaskBriefOut test constructs schema directly (doesn't hit projects.py/drama_projects.py sites); TaskDetailDialog 551 / QueuesPage 538 lines (pre-existing)
- Task 8: minor (deferred): loading vs error vs empty catalog render identically (pre-existing); dead CSS .pf-model-opt-provider; Auto sent as '' vs undefined inconsistently (backend treats same)
- Task 9: minor (deferred): stale 'Kie' fragments in dramaGenError channelCredits/rejected classifiers (match old stored error text, not user-visible)
- Task 11: no diff (all greps clean, backend 921+2 known, admin 28, frontend 7, tsc --force clean); minor (deferred): stale test docstring test_task_runtime_watchdog.py:157 mentions TokenFree
- Final review (Plan C): With fixes — 0 Critical, 3 Important (I1 stale-snapshot provider delete [parked T4]; I2 Mô hình tab no error state; I3 PROVIDERS.md upgrade section says env seeds on upgrade — code skips), 7 minors; T12 B1/B2/untranslated labels not regressions
