SECRET_KEY=dev-secret-change-me
# DEBUG=true 时应用日志更详细；SQL_ECHO=true 才打印原始 SQL（默认关，避免刷屏）
# DEBUG=true
# SQL_ECHO=false
# 仅支持 PostgreSQL（Docker 见 deploy/docker-compose.yml）
DATABASE_URL=postgresql+asyncpg://printfilm:change-me-strong-db-password@127.0.0.1:15432/printfilm
DATABASE_URL_SYNC=postgresql+psycopg2://printfilm:change-me-strong-db-password@127.0.0.1:15432/printfilm
# Postgres 生产建议：API uvicorn --workers 1；Celery 设 PRINTFILM_DB_ROLE=celery
# 生产建议与任务并发对齐（杭州现网：100+20，PG max_connections≥250）
# DB_POOL_SIZE=100
# DB_MAX_OVERFLOW=20
# DB_POOL_SIZE_CELERY=2
# DB_MAX_OVERFLOW_CELERY=3
REDIS_URL=redis://127.0.0.1:16379/0
CORS_ORIGINS=http://localhost:5173,http://127.0.0.1:5173,http://localhost:5174,http://127.0.0.1:5174
PUBLIC_BASE_URL=http://127.0.0.1:8000

# 启动时将已有账号提升为 admin（逗号分隔邮箱，不造号）
# ADMIN_BOOTSTRAP_EMAILS=admin@example.com

ARK_MOCK=false
# 以下 provider 配置只在首次启动（DB 尚无任何 provider）时用于 seed；
# 之后请到管理后台「系统设置 → Model routing」维护 provider 与 function bindings，改 .env 不再生效。
OPENAI_API_KEY=replace-me
OPENAI_BASE_URL=https://api.openai.com/v1
MODEL_LLM=gpt-5.6-sol
ARK_API_KEY=replace-me
ARK_BASE_URL=https://ark.ap-southeast.bytepluses.com/api/v3

USE_CELERY=false
QUOTA_ENABLED=false

# Token 计费（用户价 = TokenFree 官方成本，不再加价）
BILLING_ENABLED=false
BILLING_MARKUP=1.0
# TokenFree / New API：500000 quota = 1 USD，按此汇率折人民币分
BILLING_USD_CNY=7.0
BILLING_SIGNUP_GRANT_FEN=500
# 易支付 pay.gitcc.com（密钥勿提交）
# 展示货币（钱包内部按分记账）：默认 VND，可切 USD；汇率 1 CNY = BILLING_CNY_VND
BILLING_DISPLAY_CURRENCY=VND
BILLING_CNY_VND=3600
# 银行转账收款（建议在管理后台「支付与汇率」填写；BIN 用于生成 VietQR）
TOPUP_BANK_NAME=
TOPUP_BANK_ACCOUNT=
TOPUP_BANK_HOLDER=
TOPUP_BANK_BIN=
TOPUP_ORDER_EXPIRE_HOURS=24
# 真实 OSS / 生产域名见本机 backend/.env.secrets.local（勿提交）

# 旗舰模型（需在 BytePlus ModelArk 控制台开通；若账号仅支持接入点，可改为 ep-xxxxxxxx）
# 文字模型见上方 OPENAI_* / MODEL_LLM；以下为 Seedream/Seedance/TTS
MODEL_IMAGE=dola-seedream-5-0-pro-260628
# Seedream 4.5 接入点（可选；未配则回退 MODEL_IMAGE）
# MODEL_IMAGE_45=ep-xxxxxxxx
MODEL_VIDEO=dreamina-seedance-2-5-260628
# Seedance 2.0 接入点（可选；与 2.5 并存时前台可选 seedance-2）
# MODEL_VIDEO_2=dreamina-seedance-2-0-260128
SEEDANCE_DURATION_MIN=4
SEEDANCE_DURATION_MAX=30
# 科普 Seedance 仍出音轨：只要键盘/点击等操作环境音，不要口播与 BGM
KEPU_SEEDANCE_SFX_AUDIO=true
# 音频/TTS 上游模型 ID（OpenAI 兼容 provider）
MODEL_AUDIO=gpt-4o-mini-tts

# 豆包语音合成（openspeech，与 TokenFree Key 不同）。S_ 克隆音色仍走此项；未配则 TokenFree 走 /audio/speech
# 新版控制台 API Key（ai-manju 项目里同名 SPEECH_API_KEY）
# VOLC_TTS_API_KEY=
# 旧版控制台（可选回退）
# VOLC_TTS_APP_ID=
# VOLC_TTS_ACCESS_KEY=
# VOLC_TTS_RESOURCE_ID=seed-tts-2.0
# VOLC_TTS_SPEAKER=zh_female_cancan_uranus_bigtts
# 漫剧角色音色设计（需控制台购买 S_ 槽位）：https://docs.volcengine.com/docs/6561/2277844
# VOLC_TTS_VOICE_DESIGN_SPEAKER_IDS=S_xxx,S_yyy
# VOLC_TTS_VOICE_DESIGN_URL=https://openspeech.bytedance.com/api/v3/tts/voice_design

# 上游用量对照改走 TokenFree New API（与「模型」页同一把 Key），无需火山管控面 AK/SK
# BILLING_USD_CNY=7.0

ARK_IMAGE_SIZE=2k
ARK_VIDEO_RESOLUTION=480p
ARK_VIDEO_RATIO=16:9
ARK_VIDEO_POLL_INTERVAL=8
ARK_VIDEO_POLL_TIMEOUT=900
PIPELINE_IMAGE_CONCURRENCY=3
PIPELINE_VIDEO_CONCURRENCY=10
PIPELINE_AUDIO_CONCURRENCY=4

# Aliyun OSS（成片/分镜上传；FFmpeg 仍用本地文件。前端 dist 见 deploy/scripts/upload_oss_web.py）
OSS_ENABLED=false
OSS_ENDPOINT=oss-cn-beijing.aliyuncs.com
OSS_REGION=cn-hangzhou
OSS_BUCKET=your-bucket
OSS_FOLDER=kepu
OSS_ACCESS_KEY_ID=replace-me
OSS_ACCESS_KEY_SECRET=replace-me
# OSS_PUBLIC_BASE=https://cdn.example.com
